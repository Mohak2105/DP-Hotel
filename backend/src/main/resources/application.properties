server.port=5001

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/dp_hotel?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mohak@664252
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration (at least 256-bit secret for HS256)
app.jwt.secret=your_super_secret_jwt_key_change_in_production_dp_hotel_super_secure_key_2026
app.jwt.expiration-ms=604800000

# CORS Configuration
app.cors.allowed-origins=http://localhost:5173,http://localhost:8080,http://localhost:8081,http://localhost:3000

# Jackson Settings
spring.jackson.deserialization.fail-on-unknown-properties=false

# Cashfree Payment Gateway Configuration
cashfree.app-id=${CASHFREE_APP_ID:}
cashfree.secret-key=${CASHFREE_SECRET_KEY:}
cashfree.environment=${CASHFREE_ENVIRONMENT:sandbox}
cashfree.api-version=2023-08-01
